# Changes the remote of the evisualization-prototype github repository
# to use git instead of https. The default checkout is now using https
# which might be considered more difficult when pushing changes.

git remote set-url origin git@github.com:hugobuddel/evisualization-prototype.git
